version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - echo Installing packages...
  build:
    commands:
      - echo Build phase complete. Nothing to build for static site.
artifacts:
  files:
    - '**/*'
